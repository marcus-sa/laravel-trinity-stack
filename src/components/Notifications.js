import { Component } from 'trinity-web'

import { aside } from 'utils/dom'

export default class Notifications extends Component {

    render = () => (
        aside({ id: 'notifications '})(

        )
    )

}